#ifndef REQUEST_HPP
#define REQUEST_HPP

#include "Webserv.hpp"

/* Request es la clase que se encarga de gestionar las peticiones HTTP,
 parsea y almacena la información. */

class Request
{
	private:

	public:
		Request();
		~Request();
};

#endif